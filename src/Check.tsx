import React from 'react';
import { createIconComponent, Icon } from './IconestIcon';

export const Check: Icon = createIconComponent('Check', [
  React.createElement("path", { key: "path-0", d: "M20 6L9 17L4 12" })
]);